@page
@model IfsAcademicSystem.RazorPages.Pages.Departments.IndexModel

@{
    ViewData["Title"] = "Departamentos";
}

<h1>Departamentos</h1>

<p>
    <a asp-page="Create">Criar novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Department[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department[0].Budget)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department[0].Administrator)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Department[0].ConcurrencyToken)
			</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Department) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Budget)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Administrator.FullName)
			</td>
            <td>
                @Utility.GetLastChars(item.ConcurrencyToken)
            </td>
            
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DepartmentID">Editar</a> |
                <a asp-page="./Details" asp-route-id="@item.DepartmentID">Detalhes</a> |
                <a asp-page="./Delete" asp-route-id="@item.DepartmentID">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
